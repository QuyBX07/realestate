export interface PropertyTypeTrend {
  date: string;     // yyyy-MM-dd
  type: string;     // Loại hình
  count: number;    // Số tin đăng
  avgPrice: number; // Giá trung bình
  avgArea: number;  // Diện tích trung bình
}